export const RandomFox = ({ imageURL }: { imageURL: string }) => {
    return (
      <img
        width={300}
        height="auto"
        src={imageURL}
        alt="Random Fox"
      />
    );
  };
  
